# AdministrateMe V3

Start at **[START-HERE.md](START-HERE.md)**. There is exactly one entry point on
purpose — two would drift apart within a week.

`docs/ARCHITECTURE.md` is the source of truth. `harness.toml` is the only
control surface. `HARNESS_ENABLED=false` (Settings → Secrets and variables →
Actions → Variables) stops everything within seconds.
